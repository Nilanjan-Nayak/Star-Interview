# Project Rules & Customizations

- **Git Push Rule:** DO NOT automatically push code or sync commits to the remote GitHub repository (`git push`). Only execute `git push` when the user explicitly instructs or requests a repository push.
